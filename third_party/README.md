# Third-party reference code

This directory contains narrowly scoped upstream code retained for reference while implementing RIW extensions. Each subdirectory must include its upstream source, pinned commit, license, included-file list, and intended use.

Do not modify these snapshots in place. Adapt the required behavior in RIW-owned modules and document any differences.

